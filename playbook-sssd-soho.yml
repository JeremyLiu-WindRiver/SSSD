- name: SSSD Integration Playbook
  hosts:  all
  vars_prompt:
    - name: "bind_user"
      prompt: "Username for joining AD"
      private: no
        
    - name: "bind_password"
      prompt: "Password for the AD user"
      private: yes
     
  roles:
    - SSSD-Workstation
    - Samba
